"""Support code for the test suite.

Pytest does not collect this package. It holds the helpers that more than one
test directory shares. A helper that only one test file uses belongs beside
that file, not here.
"""
